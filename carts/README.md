# carts/

Демо-картриджи консоли. Появляются с вехи M1 (первый — «змейка», см. ROADMAP).
Каждый картридж — папка с `src/*.cs` и ассетами, упаковывается CLI-командой
`quarp pack` в один файл `.quarp8`.
