# M9 — рабочий приказ (внутренний документ оркестрации)

Критерий вехи (ADR-026, порция 1 «арт»): quarp.exe без аргументов показывает библиотеку
игр; спрайты, карта и флаги нового картриджа делаются целиком внутри Quarp — без
Aseprite, Tiled и ручного CSV; round-trip арта существующих демо байт-в-байт;
финальная приёмка — владелец сам рисует мини-уровень и говорит «удобно».

Смысл вехи. M4 доказал, что на консоли можно СДЕЛАТЬ игру; M9 делает так, чтобы её
было ПРИЯТНО делать — и чтобы консоль встречала человека лицом (библиотекой игр), а не тестовой
радугой. Четыре независимых костыля бригад M4 (парсеры hex-строк, генераторы карт)
— это спецификация инструмента, который здесь строится.

Решение владельца о курсе (ADR-025): только Windows до финальных этапов; кросс-арх
CI остаётся как доказательство детерминизма. Лесенка редакторов (ADR-026): сейчас
арт, затем звук, потом код — эта веха несёт только порцию «арт».

## Уроки прошлых вех (обязательно каждой бригаде; полные тексты — M4-WORKORDER §1)

1. Приказ — не истина: числа и имена сверять с первоисточником, расхождение — в отчёт.
2. Зелёный тест ничего не стоит, пока не показано красное (негативный контроль
   с возвратом по sha256).
3. Отчёт без личного прогона недействителен.
4. Параллельные бригады не запускают dotnet (дерутся за obj/); компилирует организатор.
5. Убивать только свои процессы и только по PID; чужой процесс — строка в отчёте.
6. Якоря не двигать молча.
7. НОВОЕ ДЛЯ M9 — ни один видимый пиксель UI не едет в релиз мимо глаза владельца
   (урок шрифтов M4, куплен дважды): вёрстка, иконки, цвета библиотеки и редакторов
   показываются владельцу скриншотом/батником ДО приёмки этапа.

## Якоря вехи (не должны измениться ни на одном этапе M9)

Все 8 значений scripts/check-anchors.sh (таблица — PLAYBOOK §4) и все 12 хэшей
carts/demo-goldens.tsv. Редакторы живут в оболочке и не имеют права трогать путь
симуляции: внутренности Quarp.Core и библиотека Quarp.Api.Std в этой вехе меняются
только по явному решению организатора с обоснованием в приказе этапа, а **железная
поверхность API-8 (IConsoleApi, зеркала Cartridge, Q, Fix/SMath/Button/Font) —
только через ADR (ADR-024); приказная оговорка её не заменяет**.

Форматы файлов (map.bin, flags.bin, gfx.png, .quarp8) НЕ меняются: редактор — новый
писатель существующих форматов, не новый формат. Доказательство — round-trip
байт-в-байт на арте демо. Исключение возможно только через ADR (например, «# width N»
из debt-map-csv-hand-authoring — решить на этапе карт).

## Карточки очереди, которые несёт эта веха

- idea-boot-menu (библиотека игр — теперь продукт этапа 1, не костыль);
- debt-map-csv-hand-authoring (редактор карт закрывает боль; судьба «# width N» —
  решение этапа карт);
- debt-flags-authoring (флаги авторятся в редакторе карт);
- idea-gfx-txt (вероятно закрывается редактором спрайтов — решить на этапе спрайтов:
  текстовый конвейер может стать ненужным);
- debt-demo-polish п.6 (поведение голого quarp — библиотека меняет ответ).

# Этап 1. Скелет оболочки: три режима, библиотека игр, спайк UI

Режимы названы владельцем (гриль входа, уточнение): **библиотека игр ↔ игра ↔
редактор игр**. Прецеденты ниши — Splore (PICO-8) и SURF (TIC-80): встроенная
библиотека картриджей — норма жанра; загрузка сразу в неё — наше следствие
ADR-009 (терминала-самохоста у нас нет).

## Что здесь считается сделанным

1. У оболочки появляются режимы: Библиотека ↔ Игра (↔ Редактор — заглушка
   перехода). Запуск quarp.exe без аргументов → библиотека: список картриджей
   (папки carts/ рядом с exe и в рабочей папке, И `.quarp8`-файлы — наравне,
   оба пути запуска полноправны), стрелки + Z/Enter — запуск, Esc из игры —
   обратно в библиотеку. `quarp run <cart>` работает как раньше — прямой запуск;
   **Esc в прямом запуске = выход из процесса, как сейчас** (это цикл
   разработчика/F5, библиотека в него не вмешивается).
   **Судьба `quarp run` без пути** (сейчас — окно с радугой, Program.cs): тоже
   библиотека — у факта «консоль без картриджа» один владелец. Оконная радуга
   умирает осознанно: палитра проверяется файлом `quarp pattern <out.bmp>`
   и самой библиотекой (она рисуется на Master32).
2. Спайк UI-библиотеки для будущих редакторов: самописный минимум поверх MonoGame
   против готовой библиотеки (ImGui.NET — первый кандидат). Выход спайка — решение
   организатора с замером (число строк на кнопку/список/поле, вид на экране,
   совместимость с нашим integer-scale рендером) и скриншот владельцу.
3. Жизненный цикл — честно НОВАЯ работа, а не переиспользование (находка оракула:
   оболочка сегодня рождает сессию в конструкторе и выгружает только на выходе
   из процесса; переходов посреди работы у неё никогда не было). Требования:
   - Esc из игры в библиотеку проходит через полный Dispose сессии — **save.dat
     гарантированно записан** (сейчас он пишется только в Dispose/OnExiting);
   - аудио-очередь дренируется на переходе — в библиотеке не звучит хвост игры;
   - выгрузка через collectible ALC без утечек сборок (счётчик ALC в тесте,
     красная форма — нарочная утечка).
4. Детерминизм не тронут: якоря 8/8, 12 хэшей демо, sim/replay пути без правок.

## Решения организатора

- Библиотека рисуется в НАТИВНОМ разрешении окна (это UI хоста, не виртуальный экран) —
  но стилистически на палитре Master32 и системном шрифте: лицо консоли.
- Навигация этапа 1 — клавиатура (стрелки/Z/X/Esc); мышь появится с редакторами.
- Пустой список картриджей — не радуга: подсказка «положи картридж в carts/ или
  quarp new».
- Хэдлесс-пути (sim, replay, build, pack) библиотеку НЕ показывают никогда.
  Голый `quarp` — GUI-приложение и в headless честно висит окном: это норма,
  принимается вслух; этап 1 добавляет строку в README («в скриптах — sim/replay/
  build, не голый quarp») — этим закрывается debt-demo-polish п.6 целиком.
- Библиотека = локальные файлы. Онлайн-каталог (аналог Splore-онлайн) остаётся
  в «осознанно отложено» (ROADMAP) — ссылка на Splore/SURF выше про прецедент
  встроенного браузера, не про сеть.

## Решение по спайку UI (этап 1 п.2 — ЗАКРЫТ, 2026-08-19)

**Вердикт: свои виджеты поверх PixelFontAtlas, ImGui не берём.** Числа:

- Замер на собственном дереве (волна 1, не прогноз): полный экран библиотеки —
  атлас шрифта 91 строка + рендерер со списком/плашкой/футером/строкой ошибки
  145 строк + владелец палитры 22 строки = **258 строк, ноль зависимостей**,
  нативное разрешение, лицо консоли на Master32.
- Цена ImGui-пути: NuGet-пакет + **нативная cimgui.dll** + рендерер-байндинг
  к MonoGame, который придётся владеть самим (официального бэкенда нет,
  общинные сэмплы — сотни строк), + чужой вид, требующий тем, чтобы не ломать
  лицо консоли.
- Главный аргумент — структурный: доминирующие поверхности редакторов
  (пиксельный холст, сетка карты, свотчи палитры) **кастомные в обоих мирах**;
  ImGui экономит только хром, а хром у нас уже стоит ~258 строк.

**Фальсификатор записан:** если хром этапа 2 (кнопки, свотчи, скролл-список)
превысит ~500 строк — стоп, решение пересматривается в сторону ImGui той же
процедурой. Не превысит — вопрос закрыт навсегда.

## Приёмка этапа 1

1. Сборка 0/0; полный dotnet test зелёный; якоря 8/8; 12 хэшей демо на месте. —
   **сделано волной 1** (824 теста, красные формы бригады ×5 + личный контроль
   организатора по сбросу сейва, sha256 совпал; CI run 32316966968 зелёный
   кросс-арх на коммите d198ce7).
2. Негативные контроли новых проверок показаны красными с возвратом по sha256. —
   **сделано** (таблица — в отчёте волны, лично перепроверен сброс save.dat).
3. Библиотека прогнана и показана владельцу (play/library.cmd) ДО закрытия этапа —
   правило №7. — **сделано: владелец посмотрел и сказал «ок» (2026-08-19).**
4. Решение по UI-библиотеке записано в этот приказ с числами спайка. — **сделано**
   (раздел выше).

## Этап 1 ЗАКРЫТ (2026-08-19, коммиты `d198ce7`+`529a81f`, CI 32316966968)

**Доказано** (824 теста: Core 279, CartKit 322, Analyzers 75, Shell.Desktop 72,
Cli 76; сборка 0/0; якоря 8/8; 12 хэшей демо реплеями локально и кросс-арх CI;
приёмка владельцем глазами): у консоли три режима и лицо — библиотека игр вместо
радуги; переходы честные (звук дренируется тем же кадром, save.dat дописывается
через полный Dispose, ALC умирает — доказано WeakReference с нарочной утечкой);
прямой запуск `quarp run <cart>` не изменился ни в чём (цикл F5). Пять красных
форм бригады + личный контроль организатора (сброс сейва: ровно 1 красный,
sha256 возврата совпал). Спайк UI закрыт вердиктом с замерами (раздел выше).

Уроки этапа:

1. **Автомат режимов, вынутый из окна ради тестируемости, окупился немедленно** —
   9 тестов жизненного цикла на настоящих Roslyn-компиляциях без единого окна;
   тот же приём, что CartSession/QuarpGame в M1, применённый до, а не после боли.
2. **«Переиспользуем существующий путь» — проверяй, существует ли путь целиком.**
   Оракул поймал до кода: существовала половина (уровень ALC), оболочка никогда
   не выгружала сессию посреди работы. Полдня работы бригады сэкономлены абзацем
   приказа с тремя требованиями (сейв/звук/утечка).
3. **Раздел отчёта «в чём не уверена» приносит карточки** — протекание зажатой Z
   в первый кадр игры (debt-library-launch-key-leak) найдено бригадой в её же
   сдаче, не ревьюером после.

# Этап 2. Редактор спрайтов

## Что здесь считается сделанным

1. Из библиотеки по X над выбранным картриджем открывается редактор ЕГО листа
   спрайтов: сетка листа (выбор спрайта), холст с зумом, рисование мышью.
2. Инструменты v1 — состав ФИНАЛЬНЫЙ, сверен с нишей (отчёт разведчика
   2026-08-19: таблица PICO-8/TIC-80/LIKO-12 по первоисточникам; пересечение
   трёх = минимум жанра, весь минимум взят):
   - карандаш (ЛКМ; ластик — не отдельный инструмент, а карандаш цветом 0,
     как во всех трёх консолях); пипетка (ПКМ на холсте — паттерн PICO-8);
   - заливка в границах редактируемого региона; очистка региона (Delete);
     флип H/V; поворот 90° (регион квадратный — всегда);
   - **регион редактирования 8×8 / 16×16 / 32×32** — «зум» ниши: размер
     области листа под курсором, не лупа (мультиклеточные объекты — то, что
     Spr(n,x,y,w,h) потом собирает); масштаб отображения — отдельно, простое
     целое умножение;
   - сетка листа 16×16 спрайтов (выбор региона), **16 свотчей — только
     видимая палитра** (ПОПРАВКА к первой записи «32»: ратифицированная
     SPEC-8 §6, загрузчик MapToPalette, Sset с маской & 0x0F и энкодер —
     четыре владельца факта согласны: в листе живут слоты 0–15; парные
     оттенки — runtime через Pal);
   - Undo/Redo (Ctrl+Z/Ctrl+Y), стек снапшотов листа в памяти сессии;
   - сохранение Ctrl+S; Esc с несохранёнными правками — строка-подтверждение
     в футере (сохранить и выйти / выйти без / остаться), не молчаливая
     потеря и не модальный диалог.
   Отложено с записью (idea-sprite-editor-v2): выделение+move, копипаст
   спрайтов, фигуры, размер кисти, pan, предпросмотр парных через Pal.
   Волны: **2b** — ядро (режим, мышь, холст, карандаш/пипетка/свотчи, undo,
   контракт сохранения); **2c** — преобразования (заливка, флипы, поворот,
   очистка, регионы) поверх ядра.
3. Сохранение: **чистая сессия не пишет на диск вовсе** (байт-в-байт бесплатно);
   грязная — пишет собственным ДЕТЕРМИНИРОВАННЫМ PNG-энкодером (фиксированные
   фильтр и настройки сжатия: одни пиксели → одни байты, всегда и везде).
   Пиксельный round-trip закреплён тестом: decode(encode(decode(f))) ==
   decode(f) для gfx.png всех демо; повторное сохранение того же листа — те же
   байты. У картриджа без gfx.png (змейка) редактор показывает пустой лист
   и создаёт файл первой грязной записью.
4. Мышь появляется в оболочке впервые — только в редакторе; библиотека остаётся
   клавиатурной.
5. Хром (кнопки, свотчи, скролл) считается против фальсификатора ~500 строк
   (раздел спайка выше) — число строк хрома в отчёте обязательно.

## Решения организатора

- PNG-энкодер живёт в CartKit рядом с нашим же декодером (симметрия; Shell
  не пишет форматы сам). Это НЕ железо API-8 — ADR не нужен, формат файла
  не меняется (PNG остаётся PNG, Aseprite остаётся рабочим путём — ADR-026).
- Редактируется файл картриджа-папки (carts/<имя>/gfx.png). .quarp8-файлы
  в v1 редактора не редактируются (read-only запись в библиотеке — честная
  надпись, не сюрприз при сохранении); распаковка .quarp8 в папку — не эта веха.
- Правка gfx.png под работающей игрой невозможна по построению (редактор
  и игра — разные режимы одного окна); CartWatcher увидит файл при следующем
  запуске как обычную внешнюю правку.
- Прямоугольники/фигуры, выделение, копипаст спрайтов — non-goals v1
  (карточка idea-sprite-editor-v2 уже заведена). Заливка была в этой строке
  до разведки ниши — состав п.2 (финальный, по данным) её ВКЛЮЧАЕТ; строка
  поправлена по находке волны 2c: приказ противоречил сам себе.

# Этап 2.5. Переделка UX по вердикту владельца (2026-08-19)

Владелец принял этап 2 по работоспособности и ОТКЛОНИЛ по удобству. Его макет —
закон этапа и **стандарт оболочки для всех редакторов вехи** (карты, звук, код
обязаны сесть в тот же каркас):

- **Верхняя строка — вкладки, только иконки, никакого текста.** Слева — иконка
  выхода из редактора. От правого угла справа налево: музыка, звуки, тайлмапы,
  спрайты, код (то есть слева направо у правого края: код, спрайты, тайлмапы,
  звуки, музыка). Несуществующие пока редакторы — неактивные иконки с подсказкой
  (оживают в своих порциях). Никаких заголовков: ни «SPRITE EDITOR», ни имени
  картриджа, ни имени спрайта сверху.
- **Нижняя строка — статус-бар:** координаты курсора; номер/имя выбранного
  спрайта; кликабельная иконка изменено/сохранено (клик = сохранить), рядом
  кнопки undo/redo.
- **Левая колонка — тулбар инструментов, только иконки** (как в фотошопе),
  сверху вниз: выделение (прямоугольник, кисточка), карандаш, заливка,
  штамповка, фигура (овал, прямоугольник). Ниже — строка-действия иконками:
  флип H, флип V, поворот, очистка (паритет с хоткеями F/V/R/Del).
- **Правее тулбара — область рисования; правее её колонка:** палитра, под ней
  блок управления слоями (этот этап — заглушка с одним базовым слоем; настоящие
  слои — отдельной волной: авторский файл рядом с gfx.png, сплющивание при
  сохранении — решение владельца), под ним лист спрайтов.
- **Подсказки:** наведение на любой инструмент → через 3 секунды всплывающая
  текстовая подсказка.
- **Паритет ввода — закон:** каждое действие доступно и только с клавиатуры,
  и только мышью/стилусом/пальцем.

Состав инструментов расширен вердиктом владельца (перекрывает отложенное v2):
выделение (прямоугольное и кисточкой), штамп, фигуры (овал/прямоугольник) —
теперь в составе этапа. Волны: **2d** — каркас UX (сдана 2026-08-19);
**2e** — правки каркаса по второму смотру + групповые инструменты + фигуры;
**2f** — выделение (рамка и кисть, с перемещением) + штамп.

**Поправка к пятому смотру, внесённая волной 2k (2026-08-24).** Формула ленты из пятого
смотра («4 ряда × 64 колонки, лента k=(n>>4)>>2») больше не описывает код: чтобы окно листа
выросло по приказу шестого смотра, полоса перекроена в **8 рядов × 32 колонки** (страницы
PICO-8 стопкой по две: 0-1, затем 2-3). Те же 256 спрайтов, та же идея ленты, другая форма —
иначе рост был бы обманом: полоса 16:1 в окне 2:1 заполняется только увеличением клеток,
то есть показом МЕНЬШЕ спрайтов. Владелец формулы — `SheetStrip`; результат:
без прокрутки видно **128 спрайтов вместо 48**.

**Шестой смотр владельца (2026-08-24, скриншот с разметкой): полоса листа получает
место, управление сжимается в один ряд — волна 2k.** Правая колонка перекраивается:
палитра остаётся сверху; **тогл размера спрайта и вкладки слоёв съезжают влево и встают
в ОДИН узкий ряд** — тонкая полоса между областью рисования и выбором спрайта; всё
освободившееся место под ней отдаётся **окну листа спрайтов**, которое становится шире
и выше (слайдер остаётся снизу). Пустоты справа от холста быть не должно: сегодня
половина правой колонки — воздух, а лист живёт в четырёх рядах и требует прокрутки там,
где мог бы показывать больше.

Заодно в ту же волну (найдено глазами владельца до прибора, 2026-08-24):
**переключение спрайта не имело клавиатурного пути вовсе** — `SelectRegionCell` звался
только из обработчика мыши. Это прямое нарушение закона паритета на самом ходовом
действии редактора. Закрыто: **Shift+стрелки** двигают выбранный спрайт по ленте
(шаг в координатах ленты, через `SheetStrip` — единственного владельца отображения),
полоса подкручивается ровно настолько, чтобы выбранный спрайт остался виден;
подсказки инструмента и слайдера называют клавишу. Прибор паритета (волна 2j) обязан
краснеть, если такая дыра появится снова.

**Пятый смотр владельца (2026-08-20, скриншот + фото PICO-8): окно листа —
ПОЛОСА, волна 2i.** Мёртвая зона справа и спящий слайдер — потому что квадратный
лист влезал в окно. Эталон владельца — навигатор PICO-8: лист показывается
вытянутой полосой **4 ряда спрайтов высотой × 64 колонки** (те же 256 спрайтов,
нумерация лентами: спрайт n → лента k=(n>>4)>>2, колонка полосы 16k+(n&15), ряд
полосы (n>>4)&3 — ровно страницы 0-3 PICO-8, выложенные встык); окно показывает
кусок, горизонтальный слайдер всегда живой (полоса всегда длиннее окна); у PICO-8
вместо прокрутки страницы — у нас слайдер, это и есть отличие по воле владельца.
Регион, пересекающий границу ленты, подсвечивается двумя кусками — это правильно.
Окно листа становится ниже (4 ряда + слайдер), освободившееся место под палитрой
остаётся пустым под будущие блоки.

**Четвёртый смотр владельца (2026-08-20, эскиз-скриншот): функциональная планка
взята («все инструменты работают неплохо»), раскладка дозревает — волна 2h:**
1. Палитра сдвигается вправо, к краю окна.
2. Окно листа спрайтов: ширина = ширине палитры, высота = половине свободного
   места, ГОРИЗОНТАЛЬНЫЙ слайдер-прокрутка для того, что не влезло.
3. НАСТОЯЩИЕ СЛОИ (ADR-027): 5 вкладок над окном спрайтов, каждая — свой лист;
   рисование — в активном слое, холст и окно листа показывают композит;
   слой 1 — базовый, верхние перекрывают, 0 прозрачен; носитель —
   gfx-layers.png (128×640, наш энкодер), консоль/сборка его не видят,
   сплющенный композит уезжает в gfx.png при сохранении; заглушка
   «BASE LAYER» удаляется. Undo — снапшот всей стопки (80 КБ — дёшево),
   дисциплина «операция = шаг» не меняется.
4. Тогл размера спрайта в полосе между холстом и правой колонкой: кнопка
   «8» → список 8/16/32 (наш регион; Tab-хоткей остаётся).

**Третий смотр владельца (2026-08-19, после 2f): два бага + три правки — волна 2g:**
1. БАГ: штамп не выбирается мышью — в HandleEditorButton нет ветки ToolStamp
   (кнопка есть в раскладке, нет в роутере). Класс дефекта закрывается
   контрактным тестом: каждая размещённая живая кнопка меняет наблюдаемое.
2. БАГ-засада: очистка «не работает» — выделение переживало смену инструмента
   и Clear молча чистил только его маску. Лечится п.3.
3. Выделение живёт только у инструмента выделения: смена инструмента
   коммитит плавающее и СНИМАЕТ выделение (источник штампа уже захвачен);
   оверлей не показывается ни при каком другом инструменте.
4. Вид выделения — «бегущие муравьи»: прерывистая анимированная рамка
   по границе маски, как в Photoshop/Aseprite. Белая рамка + синяя заливка —
   отменены. Стандарт оболочки.
5. ВОЛШЕБНАЯ ПАЛОЧКА — третий вариант группы выделения (владелец имел
   в виду её с самого начала): клик выделяет связную область одного цвета
   (4-связность, в границах региона). Кисточка-маска ПОНРАВИЛАСЬ — остаётся.

**Второй смотр владельца (2026-08-19, скриншот): каркас в целом лёг, три правки:**
1. Ряд действий (флипы/поворот/очистка) под тулбаром ломает лаяут — УБРАТЬ.
   Флип H, флип V и поворот объединяются в ОДИН слот тулбара группой
   с вариантами — «как фигуры», фотошоп-стиль (маркер-уголок на слоте;
   вариант — флайаутом по долгому нажатию/ПКМ; клавиатурно — повтор цифры
   циклит вариант; прямые хоткеи F/V/R остаются).
2. Кнопка очистки — вниз, в статус-бар, справа от redo (хоткей Del остаётся).
3. Верхняя строка вкладок и нижняя строка статуса ОТДЕЛЯЮТСЯ другим цветом
   фона — сейчас сливаются «в кашу» с окном. Полосы — стандарт оболочки
   для всех редакторов вехи.

**Фальсификатор хрома формально сработал и пересмотрен:** порог ~500 ставился
составу v1; вердикт владельца — рост СОСТАВА (иконки, вкладки, подсказки,
кнопки, тач), а не раздувание того же. Переоценка ImGui той же процедурой:
пиксель-иконки, консольный вид и тач-паритет ImGui бесплатно не даёт (нативная
dll + самодельный бэкенд + перекраска остаются в цене) — остаёмся на своих
виджетах. Новый порог: **хром полного каркаса с вкладками ≤ ~1500 строк**;
пробит — ре-спайк ImGui с замером.

**Вердикт по фальсификатору вынесен (2026-08-24).** У числа наконец появился владелец —
`scripts/count-chrome.sh` (метод, список файлов и разбор смешанных файлов по методам — в шапке
прибора): на дереве этапа хром = **1585 строк**, порог ~1500 пройден на 85 (5,7%). Числа волн
2g (1815) и 2h (1371) признаны недостоверными: их источника в дереве нет, а метод счёта менялся
между волнами молча — тот же класс дефекта, что молчаливый перепин якоря, только для
фальсификатора. Решение владельца (2026-08-24): **пока можно обойтись своим — третью сторону
не берём**; ImGui к рассмотрению не возвращается ни на этом превышении, ни на следующих.
Причина превышения — рост СОСТАВА, заказанный владельцем на смотрах (иконки, вкладки,
подсказки, паритет пальцем), а не раздувание того же кода, и ничего из этого ImGui даром
не даёт. Поэтому фальсификатор **меняет смысл, а не отменяется**: он перестаёт быть спусковым
крючком «взять чужую библиотеку» и становится бюджетом сложности собственного хрома. Порог
перепинен осознанно: **≤ 1800 строк по прибору** (запас на редактор карт и панель флагов),
живёт в одном месте — константе `THRESHOLD` скрипта; пробитие означает волну упрощения своего
кода, а не смену библиотеки.

## Приёмка этапа 2 (единая, после 2d+2e)

1. Сборка 0/0; полный тест зелёный; якоря 8/8; 12 хэшей демо на месте (арт демо
   в репозитории НЕ менялся — редактор его открывал, но чистая сессия не пишет).
   — **прогон организатора 2026-08-24, Windows, SDK 10.0.100-rc.2**: `dotnet build -c Release`
   0 ошибок и **1 предупреждение** (MSB3106, строгое имя System.Numerics.Vectors в
   Quarp.Analyzers — путь в локальном кэше NuGet, не наш код: «0/0» буквой не выполнено,
   расхождение записано, карточка заведена); `dotnet test` — **1102 зелёных** (Core 279,
   Analyzers 75, CartKit 333, Shell.Desktop 339, Cli 76); `scripts/check-anchors.sh` — **8/8**;
   12 хэшей `carts/demo-goldens.tsv` прогнаны локально шестью реплеями — все шесть треков совпали до бита (`ok` ×6, exit 0).
2. Негативные контроли: сломанный энкодер → красный round-trip; недетерминизм
   (перемешанные настройки) → красный тест повторного сохранения; возвраты sha256.
   — **сделано волнами 2a–2i** (таблицы в отчётах волн); прибор хрома сдан с двумя
   контролями: порог 2000 → зелёно, искажённая сигнатура метода-маркера → exit 2
   «МАРКЕР НЕ НАЙДЕН», возврат файла по sha256 совпал.
3. Правило №7: редактор показан владельцу до закрытия этапа; владелец рисует
   спрайт сам и принимает ПО УДОБСТВУ (первая приёмка 2026-08-19: работает — да,
   удобно — нет; вердикт-макет выше). — **ЖДЁТ ВЛАДЕЛЬЦА** (после волны 2i вердикта нет).
4. Число строк хрома против нового фальсификатора ~1500 — в отчёте волны и здесь.
   — **сделано**: 1585 строк прибором `scripts/count-chrome.sh`, вердикт и перепин порога —
   в разделе фальсификатора выше.
5. Паритет ввода проверен обоими способами: сценарий «нарисовать и сохранить
   спрайт» пройден один раз только клавиатурой, один раз только мышью.
   — **ЖДЁТ ВЛАДЕЛЬЦА**: сквозного теста нет, паритет держится на конструкции
   (подсказка каждой живой кнопки называет клавишный путь) и на глазах.

**Открытие, записанное вслух (оракул, 2026-08-24):** ни у одного демо в репозитории нет
`gfx.png` — все пять строят спрайты кодом из hex-строк (четыре независимые копии парсера,
карточка `idea-gfx-txt`). Значит тест round-trip арта сегодня гоняет ПУСТЫЕ листы, а критерий
ADR-026(в) «round-trip арта демо байт-в-байт» пока не имеет субстрата. Пункт 1 приёмки
«арт демо не менялся» выполнен тривиально — менять было нечего. Переезд арта демо в файлы —
решение владельца; до него критерий считается формально не проверенным.

Дальнейшие этапы (порядок предварительный, уточняется после спайка):
этап 2 — редактор спрайтов; этап 3 — редактор карт + флаги; этап 4 — полный цикл
(новый мини-картридж целиком внутри Quarp) + приёмка владельцем «удобно».

# Этап 3. Редактор карт и флаги

Вкладка тайлмапов оживает: редактор карты (map.bin) того же картриджа, в том же окне.
Флаги спрайтов (flags.bin) — **в редакторе СПРАЙТОВ**, см. поправку владельца ниже. Каркас — стандарт этапа 2.5 (верхняя строка вкладок-иконок, левый
тулбар, статус-бар, подсказки через 3 секунды, полный паритет ввода): раздел 2.5 выше — закон
этого этапа, здесь не пересказывается. Единственные карты в дереве — carts/digger и
carts/platformer, и обе пинованы в carts/demo-goldens.tsv: главный риск этапа — случайно
записать чужую карту, и приказ закрывает его дважды (решение про map.csv ниже + тест
«чистая сессия не пишет» с негативным контролем).

Критерий этапа, прибором: карта и флаги нового картриджа делаются целиком внутри Quarp —
без Tiled и ручного CSV; round-trip байт-в-байт на map.bin digger и platformer закреплён
тестом; чистая сессия не пишет на диск вовсе (у демо это и есть «байт-в-байт бесплатно» —
прецедент этапа 2). Глазами остаётся ровно то, что глазами и должно быть: приёмка владельцем
по удобству и вид каждого пикселя UI (правило №7).

**Поправка владельца (2026-08-24): панель флагов переезжает в редактор спрайтов.**
Разведка по первоисточникам (PICO-8 — мануал Lexaloffle, TIC-80 — вики и тикет мейнтейнера
#433, LIKO-12 — код `sprite.lua`) показала: во всех трёх консолях жанра флаги живут
в редакторе СПРАЙТОВ и ни в одной не редактируются в редакторе карт; желание притащить их
в карту висит открытым тикетом у самого мейнтейнера TIC-80 годами. Вердикт владельца —
«делаем как у других». Шапка вехи («флаги авторятся в редакторе карт») этим перекрыта:
флаг — свойство спрайта, и ставится он там, где спрайт выбран и виден. Критерий ADR-026
(«спрайты, карта и флаги нового картриджа делаются целиком внутри Quarp») не меняется —
меняется только вкладка, на которой это делается.

Следствия, обязательные к исполнению:
- владелец банки `flags.bin` — сессия редактора СПРАЙТОВ (у неё уже есть gfx.png и слои);
  `MapEditorSession` после волны 3b держит только карту и ни байта флагов;
- редактор карт флаги не читает и не пишет; заливок «по флагу» нет ни у кого в нише — не
  изобретаем;
- правило «чистая сессия не пишет» и проверка длины (256 байт) переезжают вместе с банкой,
  вместе со своими тестами: перенос обязан быть доказан теми же тестами, что уже зелены,
  а не переписанными заново.

## Что здесь считается сделанным

1. TilemapTab в EditorIcons перестаёт быть неактивной: из редактора спрайтов по вкладке —
   редактор карты того же картриджа и обратно, без потери несохранённого в обеих сессиях.
2. Полотно карты 256×72 (MAP-FORMAT §2): окно показывает кусок, навигация по всей карте
   обязана существовать и клавиатурой, и мышью (паритет — закон 2.5). Выбор тайла — из листа
   спрайтов картриджа (полоса, как в редакторе спрайтов). Тайл 0 — пустота, а не спрайт 0
   (MAP-FORMAT §2): пикер показывает клетку 0 честно пустой, что бы ни лежало в спрайте 0.
3. Инструменты v1 — тем же методом, что на этапе 2: разведка ниши по первоисточникам
   (PICO-8/TIC-80/LIKO-12), пересечение = минимум жанра, минимум берётся весь. Ожидаемый
   состав — карандаш, пипетка (ПКМ), заливка, многотайловый штамп с выбором региона в листе —
   сверить разведкой, не верить приказу. Undo/Redo («операция = шаг»), Ctrl+S, Esc
   с несохранёнными правками — футер-подтверждение, как в редакторе спрайтов.
4. Панель флагов — **в редакторе спрайтов** (поправка владельца выше): 8 флагов на спрайт,
   256 спрайтов, ряд переключаемых значков рядом с выбранным спрайтом, обоими способами
   ввода. Носитель — flags.bin, ровно 256 байт (SPEC-8 §6), банка сессии редактора спрайтов.
   Вид панели — видимые пиксели, то есть смотр владельца, не приказ.
5. Сохранение: чистая сессия не пишет на диск вовсе — прецедент SpriteEditorSession (dirty —
   это содержимое против диска, не история правок) обязателен и здесь, по-файлово. Грязная
   запись пишет существующие форматы без единого нового байта смысла: map.bin — ровно
   18 432 байта, тайлы построчно, без магии и версии (MAP-FORMAT §1–§2); flags.bin — ровно
   256 байт (SPEC-8 §6). Писатель — не энкодер: payload и есть файл, нового кода формата
   не заводить, проверка длины перед записью обязательна. Файлы создаются только первой
   грязной записью; у нового картриджа их нет, и «всё нули при отсутствующем файле» — не
   грязно (отсутствующий ассет = нули, SPEC-8 §6).
6. Round-trip закреплён тестом: открыть map.bin digger и platformer в сессии, сохранить —
   те же байты; плюс тест «чистая сессия не пишет ни одного файла».

## Решения организатора

- **Картридж с map.csv (digger, platformer) — карта read-only.** У факта «карта картриджа»
  один владелец; пока в папке лежит map.csv, владелец — Tiled-путь, и грязная запись map.bin
  молча протухлила бы источник (тот же класс лжи, что map.csv без map.bin, который
  RequireBuiltAsset уже отвергает). Честная надпись в редакторе, не сюрприз при сохранении —
  прецедент read-only .quarp8 этапа 2. Хочешь править карту digger внутри Quarp — осознанно
  убери map.csv. Заодно это вторая линия защиты голденов.
- **«# width N» — НЕТ.** Боль debt-map-csv-hand-authoring (18 432 числа руками) закрывается
  этим редактором, а не вторым диалектом текста: усечённая геометрия развела бы наш CSV
  с экспортом Tiled (четвёртое требование MAP-FORMAT §4) и конфликтует с Р11 («неполная
  строка не добивается нулями»). Минимум карточки исполняется здесь же: §4 говорит вслух
  «ручной текст — для точечных правок; настоящий уровень — Tiled или встроенный редактор»
  (документная правка до ратификации — карточка это разрешает). Карточка закрывается.
- **debt-flags-authoring закрывается третьим путём — GUI-панелью** (шапка вехи это уже
  назначила: «флаги авторятся в редакторе карт»). Конвейер flags.txt не строится; MAP-FORMAT
  §10 дополняется строкой «флаги ставятся в редакторе карт или кодом через Fset». Fset-путь
  демо не трогается: ни один существующий картридж flags.bin не получает.
- **Раскладка не меняется — ADR не нужен.** Если удобство потребует хранить размер карты
  внутри map.bin (та самая ширина) — это смена раскладки: стоп, ADR, вопрос владельцу.
  В этот этап такое решение не входит и здесь не предрешено.
- **Зоны.** Новые файлы бригады: MapEditorSession.cs, MapEditorRenderer.cs (+ layout при
  нужде) и их тесты — по образцу тройки редактора спрайтов. Общие швы остаются у организатора
  (проверено по дереву): ShellModeMachine.cs (режимы и переходы), QuarpGame.cs (проводка),
  EditorIcons.cs (оживление TilemapTab, новые иконки). Общий хром (EditorIconAtlas,
  IconHoverTracker, SheetScroll, ToolbarFlyout, EditorMouse, PixelFontAtlas) не форкается:
  нужна правка — строка организатору, не копия у себя.
- **Контракт после поправки о флагах** (фиксируется до раздачи): банка `map.bin` (18 432 байта)
  принадлежит сессии редактора карт, банка `flags.bin` (256 байт) — сессии редактора спрайтов.
  Обе живут по одному правилу: dirty — содержимое против диска, по-файлово; Save пишет только
  грязное; чистая сессия не пишет вовсе; длина payload'а проверяется на входе и на выходе одним
  владельцем проверки. Undo у каждой сессии свой, шаг = операция. Ни одна сессия не читает чужую
  банку. Волна 3a сдала обе банки в `MapEditorSession` (поправка пришла после сдачи) — волна 3b
  переносит банку флагов в сессию спрайтов ВМЕСТЕ с её зелёными тестами, без переписывания
  правил заново. Волны — по образцу этапа 2: сначала ядро (полотно, карандаш/пипетка,
  undo, контракт сохранения), затем остальные инструменты и панель флагов поверх.

## Приёмка этапа 3

1. Сборка 0/0; полный тест зелёный; якоря 8/8; 12 хэшей carts/demo-goldens.tsv на месте;
   sha256 carts/digger/map.bin и carts/platformer/map.bin в дереве не изменились (редактор
   их открывал в тестах — чистая сессия не пишет).
2. Негативные контроли с возвратом по sha256: (а) нарочно заставь чистую сессию писать —
   тест «не пишет» красный; (б) испорти один байт на пути записи — round-trip красный;
   (в) сломай проверку длины — тест длины красный. Каждый красный вывод — дословно в отчёте.
3. Правило №7: редактор карт и панель флагов показаны владельцу ДО закрытия этапа;
   владелец сам кладёт тайлы и ставит флаг.
4. Паритет ввода: сценарий «выбрать тайл, поставить клетки, включить флаг, сохранить»
   пройден один раз только клавиатурой, один раз только мышью.
5. Число строк хрома против порога ~1500 (фальсификатор этапа 2.5) — в отчёте волны и здесь;
   прибор — scripts/count-chrome.sh (делает параллельная бригада; на HEAD этого приказа
   скрипта ещё нет — приёмка ждёт прибор, на глаз не считаем).
6. Обе карточки закрыты по-настоящему: правки MAP-FORMAT §4 и §10 в дереве, файлы
   tasks/open/debt-map-csv-hand-authoring.md и tasks/open/debt-flags-authoring.md удалены,
   строки в tasks/DONE.md.
