Зона: carts/snake/** (волна 1б этапа 4.0; после 1а)
Майка: M — самая рисковая зона переезда

# Змейка на 160×90: поле 20×10, новый эталон 3000 тиков

По карте оракула: main.cs:15-20,78-80,392-410 — GridW/GridH/CellCount от API
(поле 20×10 = 200 клеток; чётность GridH сохранена); ВНИМАНИЕ — массивы _bodyX/
_bodyY/_cellTaken инициализируются от const в полях, а API до attach бросает →
аллокация переезжает в Init (TickPathAllocatesNothing — судья); панель game-over
от API; полоска 2 px (ряды 88–90) — осознанно. plan-golden.cs:41-43,195-210 —
дубликаты GridW/GridH и цикл NextDir в lockstep (гамильтонов цикл 20×10).
Перегенерировать golden.input (план на 3000 тиков, Р22) → replay record →
golden.qrpr; README реплеев — новые числа и хэши.

Критерии: реплей 3000 тиков, 150 точек ВСЕ различны (жива до конца — на новом
поле холостая смерть позже 64-го тика, счёт яблок будет другой — записать);
regen: перезапись из golden.input побайтово стабильна.

Non-goals: всё вне carts/snake; длину голдена не менять (Р22); CartIdentity
не трогается по построению (меняется текст → identity меняется законно вместе
с реплеем — это НЕ нарушение, зафиксировать в отчёте новое значение).
