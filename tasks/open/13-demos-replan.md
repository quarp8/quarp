Зона: carts/{dialogue,shmup,breakout}/replays/** + их README (волна 3а; после волны 1)
Майка: L (замеряющая пачка — треки+хэши трёх демо; больше двух замеров не совмещать с другим)

# Треки dialogue/shmup/breakout: перепланировка на 160×90

Оракул: у всех трёх геймплей зависит от размеров экрана — треки инвалидированы
МОЛЧА (CI это не красит). Dialogue: пагинация 26→34 колонки, прохождение ~15-16
окон вместо 21, выбор ветки уедет — оба трека заново; shmup: геометрия боя от
экрана (main.cs:205-207,312,449) — walkthrough заново; breakout: ракетка
ScreenWidth/4, сетка кирпичей от экрана — walkthrough заново.

Критерии (Р20 повторно, на каждый): трек проходит до конечного состояния без
исключений; хэш ≠ холостому той же длины; у dialogue две ветки дают три разных
хэша; README-протоколы — новые числа с выводом.

Non-goals: src/main.cs демо НЕ ПРАВИТЬ (Р25 — переезд бесплатен для кода; упёрся
в баг кода на новом разрешении — СТОП и доклад, не чинить молча).
