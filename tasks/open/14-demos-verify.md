Зона: carts/{platformer,digger}/replays+README, play/ (волна 3б; после волны 1)
Майка: M

# Platformer/digger: переверификация треков; чистка play/

Оракул: их треки вероятно живы (Screen* только в камере/HUD) — но это grep, не
прогон. Прогнать оба walkthrough на 160×90: живы — обновить только хэши/числа
README (окно реакции платформера на 160×90 теперь ЕДИНСТВЕННОЕ число — таблицы
камер упростить с пометкой «после ADR-021»); мертвы — перепланировать (планер
платформера есть: tools/plan-walkthrough.py). Digger — живой показ полоски 2 px:
задокументировать словами в README как образец рецепта. play/: удалить пять
*-160x90.cmd (Р21), README папки поправить.

Критерии: оба трека проходят, хэш ≠ холостому; fall.input платформера даёт смерть;
play/ содержит по одному батнику на игру.

Non-goals: src/main.cs демо не править (Р25); батники не переименовывать.
