Зона: решение не принято — кандидаты: docs/MAP-FORMAT.md §10 / новый flags.txt-конвейер
Майка: S

# Флаги спрайтов авторить нечем

Находка бригады G (этап 3): flags.bin загрузчик читает, но текстового конвейера
(flags.txt → flags.bin, по образцу sfx/music/map) не существует; единственный
путь — Fset в Init(). При этом MAP-FORMAT §10 отправляет автора именно во флаги
(flagFilter у Map), не говоря, чем их сделать. Бригада P упёрлась в то же
независимо (SPEC-8 §6 перечисляет flags.bin — «файл, который нечем собрать»)
и тоже ушла в Fset; вторая формулировка решения — явная запись в SPEC-8,
что флаги ставятся кодом.

Решить на этапе 4: либо конвейер flags.txt (симметрия с остальными банками),
либо MAP-FORMAT §10 честно говорит «флаги задаются кодом через Fset» с примером.
Пока библиотека не решена — не делать: Fset в Init работает (копатель живёт так).

Критерий: автор с README включает flagFilter у Map, не задавая нам вопросов.

Non-goals: раскладку flags.bin не менять; в лимит кода флаги не вносить без ADR.
