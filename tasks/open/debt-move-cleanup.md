Зона: carts/*/src (комментарии — двигают CartIdentity!), plan-golden.cs, тест паттерна
Майка: S (пачкой, в момент следующего осознанного пересбора демо/голдена)

# Хвосты переезда, отложенные из-за CartIdentity — находки адверсария 4.0

Всё ниже — комментарии/утверждения, чья правка сдвигает CartIdentity картриджа
(и для змейки — sha256 голдена), поэтому чинится ПАЧКОЙ при следующем осознанном
пересборе, не поштучно:

1. **plan-golden.cs: off-by-one планировщика против рекордера** (запись T
   применяется рекордером на тике T+1, планировщик применяет на T; жив на одном
   тике запаса MinStepInterval=3). Предупреждение вписано в шапку файла; починка
   выравнивания = новые байты golden.input = перепин sha256. Чинить вместе со
   следующим пересбором голдена + добавить сверку «прогноз планировщика ==
   финальный хэш записи».
2. **ЗАКРЫТО (2026-08-19, фикс-волна ревью адверсария 4.1).** Комментарии пяти демо
   ссылались на удалённый спайк (--profile 8w, «128x72
   and 160x90», «eight screens at 128x72»): dialogue main.cs:16, platformer :10,:31,
   shmup :203, breakout :17, digger :729-730 — все шесть сняты конверсией на
   библиотеку (M4 этап 4.1, коммит `790ab9a`): комментарии переписаны заодно с
   переводом на Std, CartIdentity всех пяти уже сдвинута этой конверсией.
   Остаток, не задетый той волной, закрыт фикс-волной ревью адверсария 4.1
   (2026-08-19): `platformer main.cs:22-23` («одно число» — переписан под
   160x90/12.33 тика, 128x72/9.33 тика оставлен как история ADR-021) и
   `dialogue main.cs:215` (`_horizonY=20 for the 128x72 layout` — число стухло
   после переезда, заменено на текущее производное значение, 29 px при 160x90).
   Два упоминания «128x72» остаются в carts/*/src нарочно — оба легитимная
   история ADR-021, не спайк-хвост: `dialogue main.cs:17` (не тронуто) и новый
   `platformer main.cs:24` (появился при переписывании «одного числа» самим этим
   пунктом — «historical 128x72 measurement was worse still», ровно то, что
   README этого картриджа уже делает построчкой выше).
3. **snake main.cs:374-380**: комментарий «2 px spent on bottom border» — по факту
   1 px (y=89 — фон). README уже исправлен; комментарий — при следующем пересборе
   голдена (см. п.1, один заход).
4. **TestPatternTests**: «PatternFillsEveryPixel» не ловит незакрашенный пиксель
   (0 < 32 проходит); дыра в середине пройдёт. Добавить настоящий контроль
   (например, рендер на префилл 255 и проверка отсутствия 255) — тесты якорей
   не двигают, можно раньше остальных пунктов.

Критерий: grep «8w|--profile|128x72» по carts/*/src (п.2, закрыт) — пуст, кроме
двух легитимных упоминаний истории ADR-021 (`dialogue main.cs:17`,
`platformer main.cs:24`, см. выше); прогноз
планировщика сверяется с записью автоматически (п.1, открыт); негативный контроль
паттерна краснеет на дыре (п.4, открыт).

Non-goals: не чинить поштучно (каждая правка = отдельный перепин identity);
голдены без объяснения не двигать.
